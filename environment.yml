name: pdr
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - cytoolz
  - numpy
  - pandas
  - git
  - pip
  - python-Levenshtein
  - multidict
  - more-itertools
  # optional dependencies (use minimal_environment.yml if you'd prefer not to install)
  - pvl
  - astropy
  - jupyter
  - pillow
  - matplotlib
  - pip:
      - pds4-tools
      - git+https://github.com/MillionConcepts/dustgoggles.git
